# UNet inference configuration
_target_: activediff.models.unet.inference
_partial_: true

batch_size: 8
fom_eval: False
guide: ~/scratch/nanophoto/topoptim/lowfom/distselect/images.npy

# Sampling method: ddpm (1000 steps) or ddim (fewer steps, same model)
sampler: ddim
ddim_steps: 50
ddim_eta: 0.0  # 0 = deterministic, 1 ≈ DDPM variance
